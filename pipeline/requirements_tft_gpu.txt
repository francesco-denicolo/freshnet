# Requirements per il resourcing TFT su GPU cloud (CUDA).
# Installa PRIMA torch con la build CUDA giusta per la tua istanza, es. CUDA 12.1:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu121
# poi:
#   pip install -r pipeline/requirements_tft_gpu.txt
#
# Versioni allineate all'ambiente locale (torch 2.8.0 / pf 1.4.0 / lightning 2.6.0)
# per garantire che le cache TimeSeriesDataSet e i risultati siano comparabili.
pytorch-forecasting==1.4.0
lightning==2.6.0
optuna>=4.0
optuna-integration>=4.0
numpy>=1.24,<2.1
pandas>=2.0
pyarrow>=14
scikit-learn>=1.3
